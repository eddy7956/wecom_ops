from flask import jsonify, g, current_app
from . import bp

@bp.get("/health")
def health():
    return jsonify({"ok": True, "trace_id": g.trace_id, "version": current_app.config.get("API_VERSION", "v1")})
